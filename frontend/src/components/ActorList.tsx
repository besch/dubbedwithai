import { Suspense, useRef, useEffect } from "react";
import { useSelector } from "react-redux";
import { RootState } from "@/store/store";

export default function ActorList() {
  return <div className="m-5 w-2/3">ActorList</div>;
}
